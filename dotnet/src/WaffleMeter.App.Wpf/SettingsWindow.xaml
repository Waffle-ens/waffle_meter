<Window x:Class="WaffleMeter.App.Wpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="waffle_meter 설정"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI">
    <StackPanel Margin="16">
        <TextBlock Text="통계 동의" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
        <CheckBox Content="익명 통계 업로드에 동의" IsChecked="{Binding ConsentAccepted}" Margin="0,2"/>
        <CheckBox Content="업로드 활성화" IsChecked="{Binding UploadEnabled}" Margin="16,2,0,2"/>
        <CheckBox Content="캐릭터 공개" IsChecked="{Binding PublicCharacter}" Margin="16,2,0,2"/>
        <TextBlock Text="{Binding ConsentStatus}" Foreground="#FF666666" FontSize="11" Margin="0,4"/>
        <StackPanel Orientation="Horizontal" Margin="0,4">
            <Button Content="적용" Width="80" Click="OnApplyConsent" Margin="0,0,8,0"/>
            <Button Content="서버 동기화" Width="100" Click="OnRefreshConsent"/>
        </StackPanel>
        <TextBlock Text="{Binding UploadStatus}" Foreground="#FF666666" FontSize="11" Margin="0,2"/>

        <Separator Margin="0,12"/>
        <TextBlock Text="서버" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
        <TextBlock Text="서버 IP (CIDR)" FontSize="11" Foreground="#FF666666"/>
        <TextBox Text="{Binding ServerIp}" Margin="0,2,0,6"/>
        <TextBlock Text="포트" FontSize="11" Foreground="#FF666666"/>
        <TextBox Text="{Binding ServerPort}" Margin="0,2,0,6"/>
        <Button Content="저장" Width="80" HorizontalAlignment="Left" Click="OnSaveServer"/>

        <Separator Margin="0,12"/>
        <TextBlock Text="단축키" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
        <TextBlock Text="{Binding ResetHotkey, StringFormat='전투 초기화 — {0}'}" Margin="0,1"/>
        <TextBlock Text="{Binding VisibilityHotkey, StringFormat='표시/숨김 — {0}'}" Margin="0,1"/>
        <TextBlock Text="{Binding ClickThroughHotkey, StringFormat='클릭 통과 — {0}'}" Margin="0,1"/>
    </StackPanel>
</Window>
